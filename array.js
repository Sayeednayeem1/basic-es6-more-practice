// TODO first declare an array

const numbers = [11, 20, 30, 40, 50, 60, 70];

// you gotta know how to find the index of an array properties!

console.log(numbers.indexOf(10));

// TODO length of an array

console.log(numbers.length);

// change an property value by index
numbers[0] = 10;
console.log(numbers);